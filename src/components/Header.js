import React from 'react';

const Header = () => {
  const test = num => {
    1 + 1;
  };

  const person = {
    first: 'name',
  };

  return <header className="heyo asd">header</header>;
};

export default Header;
